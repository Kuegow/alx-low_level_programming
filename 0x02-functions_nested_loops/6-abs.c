#include "main.h"

/**
 * _abs - print absolute number
 * @n: number to be checked for absolute value
 *
 * Return: n if absolute, otheriwse -n
 */
int _abs(int n)
{
		if (n >= 0)
		{
			return (n);
		}
		return (-n);
}
